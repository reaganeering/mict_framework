To Run:

Make sure example.js, mict-framework.js, and mict-logic-engine.js are in the same directory.

Open a terminal or console window in that directory.

Run node example.js

Expected Output:

You should see the output in your console, with the text being shuffled every second.
